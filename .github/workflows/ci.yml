name: CI

on: { push: { branches-ignore: wip/** } }

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - run: bun i
      - run: bun lint
      - run: bun typecheck
      - run: bun build:prod